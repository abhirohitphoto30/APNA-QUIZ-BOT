{
  "name": "apna-quiz-bot",
  "version": "1.0.0",
  "description": "Telegram Quiz Bot with negative marking, time limits, anonymous polls",
  "type": "module",
  "scripts": {
    "dev": "node --env-file=.env api/webhook.js",
    "setup": "node --env-file=.env api/setup.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.9",
    "grammy": "^1.34.0",
    "nanoid": "^5.1.5"
  },
  "engines": {
    "node": ">=18"
  }
}
